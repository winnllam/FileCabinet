- @docs.each do |doc| #loop through all documents
  %h2= link_to doc.title, doc #link to doc
  %p= time_ago_in_words(doc.created_at) #time stamp (comes from db)
  %p= truncate(doc.content, length:50) #ask for snippet of doc

= link_to "Create Doc", new_doc_path #make a new doc
